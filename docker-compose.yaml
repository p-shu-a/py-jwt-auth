# This file was generated by chatGPT
version: "3.9"

services:
  api:
    build: .
    container_name: py-jwt-api
    command: uvicorn api.app:api --host 0.0.0.0 --port 8080 --reload
    ports:
      - "8080:8080"
    volumes:
      - .:/py-jwt-auth
    environment:
      - PROD_DATABASE_URL=postgresql+asyncpg://token_master:safe_word@db:5432/jwt_users
      - TEST_DATABASE_URL=postgresql+asyncpg://token_master:safe_word@db:5432/jwt_test
    depends_on:
      - db

  db:
    image: postgres:14
    container_name: jwt-db
    restart: always
    environment:
      - POSTGRES_USER=token_master
      - POSTGRES_PASSWORD=safe_word
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-multi-db.sql:/docker-entrypoint-initdb.d/init-multi-db.sql:ro


volumes:
  postgres_data: